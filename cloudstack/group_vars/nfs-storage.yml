---
# NFS 설정
nfs_export_path: /export
nfs_primary_path: /export/primary
nfs_secondary_path: /export/secondary

# NFS 접근 권한
nfs_allowed_networks:
  - "192.168.1.0/24"

# 디스크 용량 (GB)
primary_storage_size: 500
secondary_storage_size: 200
